image: python:3.11

stages:
  - lint

before_script:
  - pip install -r requirements.txt
  - pip install black isort flake8 mypy

lint:
  stage: lint
  script:
    - black --check .
    - isort --check-only .
    - flake8 .
    - mypy .